services:
  mysql:
    image: mysql:8.0
    container_name: campuslearn-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: campus_root_pass
      MYSQL_DATABASE: campus_learn
      MYSQL_USER: campus
      MYSQL_PASSWORD: campus_pass
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-ucampus", "-pcampus_pass"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s
    networks:
      - campuslearn

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: campuslearn-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql://campus:campus_pass@mysql:3306/campus_learn
      JWT_SECRET: campus-learn-jwt-secret-change-in-production-2024
      JWT_EXPIRATION_HOURS: "24"
      DB_MAX_CONNECTIONS: "10"
      ROCKET_ADDRESS: "0.0.0.0"
      ROCKET_PORT: "8000"
      RUST_LOG: info
    ports:
      - "8000:8000"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    networks:
      - campuslearn

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: campuslearn-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - campuslearn

volumes:
  mysql_data:

networks:
  campuslearn:
    driver: bridge
